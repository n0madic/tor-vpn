[package]
name = "config"
version = "0.1.0"
edition = "2021"

[dependencies]
state = { path = "../state" }
clap = { version = "4", features = ["derive", "env"] }
cidr = "0.3"
anyhow = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
which = "8"
serde_json = "1"
